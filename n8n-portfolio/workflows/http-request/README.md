# HTTP Request / API Integration

Template workflow demonstrating direct OpenAI REST API integration with file export.

## Setup
1. Replace YOUR_OPENAI_API_KEY with your actual key
2. Update the model name and prompt as needed
3. Run manually to test
